using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ProfileSystem : MonoBehaviour
{
    // Start is called before the first frame update
    void Start() {
        // clear input fields
    }

    // Update is called once per frame
    void Update() {}
    
    void setUpRegisterMenu() {
        // clear input fields
        // set username window to Z=0
        // set password1 window to Z=0
        // set password2 window to Z=0
        // set "Sign in!" button to Z=-1000
        // set "Register!" button to Z=0
    }
    
    void setUpSignInMenu() {
        // clear input fields
        // set username window to Z=0
        // set password1 window to Z=0
        // set password2 window to Z=-1000
        // set "Sign in!" button to Z=-1000
        // set "Register!" button to Z=0
    }
}
